// Setup directories
const base = global.baseDir

const cache = `${base}.cache/`
const configs = `${base}configs/`
const utils = `${base}utils/`

module.exports = {
	base,
	cache,
	configs,
	utils,
}
